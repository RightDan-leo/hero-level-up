---
description: 三版本（classic/pet/army）项目：每次修改前必须先确认作用范围
alwaysApply: true
---

# 修改前必须确认作用范围（通用 vs 单版本）

本工程是「一套地图、三个版本」的可玩广告：`classic`（经典）/ `pet`（宠物）/ `army`（士兵）。
模块边界见 `specs/architecture.md`，它是判定归类的单一事实来源。

## 红线规则（最高优先级）

**在动任何代码 / 配置 / 资源之前，必须先和用户确认本次修改的作用范围：**

- **通用修改（SHARED）**：作用于三版共用层 → 同时影响 classic / pet / army。
  例：地图/路网/数值链、Boss/龙技、输入/镜头/背景、HUD（返回/喇叭）、音效、引导、宝箱机制。
- **单版本修改（VARIANT）**：只作用于指定的某一个版本，不得影响其他两版。
  例：宠物版的化身/进化称号、士兵版的随从/敌兵、某版本的宝箱皮肤或专属演出。

## 执行方式

1. 收到需求后，**先判定归类并向用户确认**（用 AskQuestion 给出「通用 / 仅 classic / 仅 pet / 仅 army」选项），**得到确认后再动手**。
2. 需求本身已明确说明范围时，复述一句确认即可，不必反复追问。
3. **VARIANT 修改严禁改动共享层**；如果发现只能靠改共享层实现，必须停下，向用户说明并改走 `variants.ts` 的 `VariantSpec` 能力位/覆盖 + 该版本专属实体 + `GameScene` 的 `hasX` 守卫接线。
4. 改完后在结论里注明本次归类（SHARED / 哪个 variant）及受影响版本。
