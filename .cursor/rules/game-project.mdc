---
description: 
globs: 
alwaysApply: true
---

# 核心门控

## Todo（MUST）

执行任何代码修改任务时，MUST 先建立 todo 列表。最后一项 MUST 是「运行 `npm run check -- <本次修改的文件列表>` 并按输出处理」。所有 todo 完成前不得结束回合。

## 任务模式（开始前）

修改涉及 2+ 模块逻辑变更（关键词：大改/重构/改机制/重平衡）→ MUST 读取 `spec-driven-dev` SKILL，走 task 工作流。单模块/纯数值微调不触发。

## 编码门控（创建前）

创建新代码模块前 → MUST 检查 `specs/` 下有对应 spec 且状态为 `ready` 或 `implementing`，无则 STOP 先创建 spec。

创建新系统/管理器/工具类前 → MUST 读取 `src/core/index.ts`，如有匹配组件则直接 import 使用，禁止重新实现。需了解组件用法时，读取 `specs/core/` 下对应 spec 的公开接口区（`<!-- INTERNAL -->` 之前）。

## 同步组件（禁止修改）

`src/core/` 和 `src/types/core.ts` 由开发包同步维护（`node .cursor/sync.mjs pull`），禁止直接修改。需要扩展框架功能时，在 `src/utils/` 或 `src/systems/` 中扩展。

## Token 预算

通过工具写入的文件内容不在对话中重复回显，只用简短列表说明改了什么。
